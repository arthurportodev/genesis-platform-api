// Compatibility export: invitations and OTP use the same Resend transport.
export { ResendEmailTransport as ResendInvitationEmailAdapter } from '../../../common/email/resend-email.transport';
export type { ResendEmailConfig as ResendInvitationEmailConfig } from '../../../common/email/resend-email.transport';
